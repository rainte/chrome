// import hub, { HubEnum } from '../utils/hub'

export default {
  upload: async () => {
    // const data = await chrome.bookmarks.getTree()
    // return hub.set(HubEnum.Bookmark, data)
  },
  dowm: async () => {
    // const data = hub.get(HubEnum.Bookmark)
    // const tree = await chrome.bookmarks.getTree()
    // console.log('dowm', data, tree)
    // chrome.bookmarks.removeTree()
  }
}
